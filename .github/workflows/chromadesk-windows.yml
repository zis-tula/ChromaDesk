name: ChromaDesk Windows

on:
  push:
    paths:
      - 'ChromaDesk/**'
      - 'chromadesk-mcp/**'
      - 'cmake/**'
      - 'scripts/**'
      - 'WebClient/**'
      - 'CMakeLists.txt'
      - '.github/workflows/chromadesk-windows.yml'
  pull_request:
    paths:
      - 'ChromaDesk/**'
      - 'chromadesk-mcp/**'
      - 'cmake/**'
      - 'scripts/**'
      - 'WebClient/**'
      - 'CMakeLists.txt'
      - '.github/workflows/chromadesk-windows.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build
    runs-on: windows-latest
    env:
      QT_VERSION: '6.8.3'
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          arch: win64_msvc2022_64
          cache: true
          modules: 'qtmultimedia qtwebsockets'

      - name: Set Qt path
        shell: bash
        run: echo "ENV_QT_PATH=$(dirname "$QT_ROOT_DIR")" >> $GITHUB_ENV

      - name: Detect VS environment
        shell: pwsh
        run: |
          $vs_install = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -property installationPath
          echo "ENV_VS_INSTALL=$vs_install" >> $env:GITHUB_ENV

          $redist_root = Join-Path $vs_install "VC\Redist\MSVC"
          $latest = Get-ChildItem $redist_root -Directory |
            Where-Object { $_.Name -match '^\d+\.' } |
            Sort-Object Name -Descending |
            Select-Object -First 1
          echo "ENV_VCRUNTIME_VERSION=$($latest.Name)" >> $env:GITHUB_ENV

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            chromadesk-mcp/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('chromadesk-mcp/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build
        shell: cmd
        env:
          ENV_CHROMADESK_API_KEY: ${{ secrets.CHROMADESK_API_KEY }}
        run: call scripts\build_qd_win.bat Release

      - name: Build MCP Bridge
        shell: cmd
        run: call scripts\build_mcp_win.bat Release

      - name: Build Skill Host
        shell: cmd
        run: call scripts\build_skill_host_win.bat Release

      - name: Build WebClient
        shell: cmd
        run: call scripts\build_webclient_win.bat

      - name: Publish
        shell: cmd
        run: call scripts\publish_qd_win.bat Release

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup -y --no-progress

      - name: Package installer
        shell: cmd
        run: call scripts\package_qd_win.bat Release

      - name: Get version
        id: version
        shell: bash
        run: |
          echo "version=$(cat version)" >> $GITHUB_OUTPUT
          echo "ref-name=${GITHUB_REF##*/}" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChromaDesk-win-x64-${{ steps.version.outputs.ref-name }}
          path: publish/ChromaDesk-win-x64-setup.exe

      - name: Upload WebClient artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChromaDesk-WebClient-${{ steps.version.outputs.ref-name }}
          path: WebClient/dist/

      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: publish/ChromaDesk-win-x64-setup.exe
          asset_name: ChromaDesk-win-x64-${{ steps.version.outputs.ref-name }}-setup.exe
          tag: ${{ github.ref }}
          overwrite: true

      - name: Package WebClient
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        run: Compress-Archive -Path WebClient/dist/* -DestinationPath publish/ChromaDesk-WebClient.zip

      - name: Upload WebClient to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: publish/ChromaDesk-WebClient.zip
          asset_name: ChromaDesk-WebClient-${{ steps.version.outputs.ref-name }}.zip
          tag: ${{ github.ref }}
          overwrite: true
