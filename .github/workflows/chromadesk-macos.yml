name: ChromaDesk macOS

on:
  push:
    paths:
      - 'ChromaDesk/**'
      - 'chromadesk-mcp/**'
      - 'cmake/**'
      - 'scripts/**'
      - 'WebClient/**'
      - 'CMakeLists.txt'
      - '.github/workflows/chromadesk-macos.yml'
  pull_request:
    paths:
      - 'ChromaDesk/**'
      - 'chromadesk-mcp/**'
      - 'cmake/**'
      - 'scripts/**'
      - 'WebClient/**'
      - 'CMakeLists.txt'
      - '.github/workflows/chromadesk-macos.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build (${{ matrix.arch }})
    strategy:
      matrix:
        include:
          - runner: macos-14
            arch: arm64
          - runner: macos-14
            arch: x64
    runs-on: ${{ matrix.runner }}
    env:
      QT_VERSION: '6.8.3'
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          modules: 'qtmultimedia qtwebsockets'

      - name: Set Qt path
        run: echo "ENV_QT_PATH=$(dirname "$QT_ROOT_DIR")" >> $GITHUB_ENV

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.arch == 'x64' && 'x86_64-apple-darwin' || '' }}

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            chromadesk-mcp/target
            chromadesk-skill-host/target
          key: ${{ runner.os }}-${{ matrix.arch }}-cargo-${{ hashFiles('chromadesk-mcp/Cargo.lock', 'chromadesk-skill-host/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.arch }}-cargo-

      - name: Build
        env:
          ENV_CHROMADESK_API_KEY: ${{ secrets.CHROMADESK_API_KEY }}
        run: bash scripts/build_qd_mac.sh Release ${{ matrix.arch }}

      - name: Build MCP Bridge
        run: bash scripts/build_mcp_mac.sh Release ${{ matrix.arch }}

      - name: Build Skill Host
        run: bash scripts/build_skill_host_mac.sh Release ${{ matrix.arch }}

      - name: Build WebClient
        run: bash scripts/build_webclient_mac.sh

      - name: Publish
        run: bash scripts/publish_qd_mac.sh Release ${{ matrix.arch }}

      - name: Package DMG
        run: bash scripts/package_qd_mac.sh Release

      - name: Get version
        id: version
        run: |
          echo "version=$(cat version)" >> $GITHUB_OUTPUT
          echo "ref-name=${GITHUB_REF##*/}" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChromaDesk-mac-${{ matrix.arch }}-${{ steps.version.outputs.ref-name }}
          path: publish/Release/ChromaDesk.dmg

      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: publish/Release/ChromaDesk.dmg
          asset_name: ChromaDesk-mac-${{ matrix.arch }}-${{ steps.version.outputs.ref-name }}.dmg
          tag: ${{ github.ref }}
          overwrite: true
