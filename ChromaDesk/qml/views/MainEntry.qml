import QtQuick

QtObject {
    id: root
    
    // Old window for comparison during development
    property var oldWindow: Main {}
    
    // New window with redesigned UI
    property var newWindow: MainWindow {}
}
